# Pinned so a reported score can be reproduced. Versions come from installed
# distribution metadata and the set is resolved as a whole before committing.
matplotlib==3.10.8
numpy==2.4.3
pandas==2.3.3
scikit-learn==1.8.0
statsmodels==0.15.0
xgboost==3.2.0
